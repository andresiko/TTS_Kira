<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_root"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="10dp">

        <TextView
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="@string/monitor_title"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"/>

        <Button
            android:id="@+id/btnMonitorExport"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:minWidth="0dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:text="@string/monitor_export"
            android:textColor="@color/text_secondary"
            android:background="@drawable/btn_subtle"
            android:textAllCaps="false"
            android:textSize="13sp"
            android:stateListAnimator="@null"
            android:layout_marginEnd="6dp"/>

        <Button
            android:id="@+id/btnMonitorPause"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:minWidth="0dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:text="@string/monitor_pause"
            android:textColor="@color/text_secondary"
            android:textAllCaps="false"
            android:textSize="13sp"
            android:background="@drawable/btn_subtle"
            android:stateListAnimator="@null"
            android:layout_marginEnd="6dp"/>

        <Button
            android:id="@+id/btnMonitorClear"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:minWidth="0dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:text="@string/monitor_clear"
            android:textColor="@color/text_secondary"
            android:textAllCaps="false"
            android:textSize="13sp"
            android:background="@drawable/btn_subtle"
            android:stateListAnimator="@null"/>
    </LinearLayout>

    <TextView
        android:id="@+id/tvHealth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_input"
        android:padding="10dp"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:fontFamily="monospace"
        android:lineSpacingExtra="2dp"
        android:layout_marginBottom="10dp"/>

    <ScrollView
        android:id="@+id/monitorScroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@drawable/bg_log"
        android:scrollbars="vertical"
        android:fillViewport="true">

        <TextView
            android:id="@+id/tvMonitorLog"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/monitor_empty"
            android:textColor="#8FA6B8"
            android:textSize="11sp"
            android:fontFamily="monospace"
            android:lineSpacingExtra="2dp"
            android:padding="10dp"
            android:textIsSelectable="true"/>
    </ScrollView>
</LinearLayout>
